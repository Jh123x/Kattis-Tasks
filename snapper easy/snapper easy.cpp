//Snapper easy
#include <bitx/stdc++.h>
using namespace std;

int main(){
	int inputs, n, k, result;
	cin >> inputs;
	for(int i = 1; i <= inputs; i++){
		cin >> n >> k;
		if(k == 0){
			cout << "Case #" << i << " OFF\n";
			break;
		}
	}
}
