//busy schedule
#include <bits/stdc++.h>
using namespace std;

long convert(pair<string,string> p){
	int mins, hour;
	
	
	
}

bool comp(pair<string,string> p1, pair<string,string> p2){
	if(p1 == p2){
		return true;
	}else{
		
	}
}

int main(){
	long input,i;
	string formatted, time, m;
	vector<pair<string,string>> items;
	cin >> input;
	for(i = 0; i < input; i++){
		cin >> time >> m;
		items.push_back(pair<string,string>(time,m));
	}
	sort(items.begin(), items.end(), )
}
